#we'll require our config/environment.rb file to load up our environment
#"sinatra/activerecord/rake" to get Rake tasks from the sinatra-activerecord #gem.
require './config/environment'
require 'sinatra/activerecord/rake'
