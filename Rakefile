require './config/environment'   #we'll require our config/environment.rb file to load up our environment,
require 'sinatra/activerecord/rake'  #we'll require our config/environment.rb file to load up our "sinatra/activerecord/rake" to get Rake tasks from the sinatra-activerecord gem.
