require './config/environment'
# This allows rake to access the files relevant to the tasks.

require 'sinatra/activerecord/rake'
# This allows for rake tasts to be executed in the terminal, such as `create_migration` and `migrate`.